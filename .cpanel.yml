---
deployment:
  tasks:
    - export DEPLOYPATH=/home/englis21/public_html/
    - /bin/cp index.html $DEPLOYPATH
    - /bin/cp -R CSS $DEPLOYPATH
    - /bin/cp -R font $DEPLOYPATH
    - /bin/cp -R img $DEPLOYPATH
    - /bin/cp -R JS $DEPLOYPATH